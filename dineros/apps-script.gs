/**
 * ARCHIVADO — DINEROS TUMBEROS
 *
 * Este módulo dejó de funcionar como herramienta operativa.
 * El archivo activo fue retirado del branch principal para que el repositorio
 * no conserve IDs de planillas ni handlers públicos de carga.
 *
 * No desplegar este archivo como aplicación web.
 *
 * Nota: los despliegues existentes de Google Apps Script se administran fuera
 * de GitHub y deben desactivarse desde la cuenta de Google correspondiente.
 */
